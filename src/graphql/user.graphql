type User {
    id: ID!
    name: String!
    createdAt: String!
    updatedAt: String!
}

input UpdatedUser {
    name: String!
}

type Query {
    getMe: User!
}

type Mutation {
    updateMe(input: UpdatedUser!): User!
}
